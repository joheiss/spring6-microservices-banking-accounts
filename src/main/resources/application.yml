server:
    port: 8091
spring:
    application:
        name: accounts
    profiles:
        active: prod
    datasource:
        url: jdbc:h2:mem:testdb
        driverClassName: org.h2.Driver
        username: sa
        password: ''
    h2:
        console:
            enabled: true
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate:
            ddl-auto: update
        show-sql: true
    config:
        import: "optional:configserver:http://localhost:8071/"
    rabbitmq:
        host: localhost
        port: 5672
        username: guest
        password: guest

management:
    endpoints:
        web:
            exposure:
                include: "*"
                # include: "refresh"
                # include: "bus-refresh"

# --- MOVED TO CLOUD CONFIG SERVER
#     config:
#         import:
#             - "application_test.yml"
#             - "application_prod.yml"
#     profiles:
#         active:
#             - "test"

# build:
#     version: "1.0.0"

# accounts:
#     message: "Learning how to create microservices - here: accounts"
#     contact:
#         name: "Hansi Hampelmann"
#         email: "hansi@heinzi.de"
#     support:
#         - +49 171 44455556
#         - +49 800 77778888